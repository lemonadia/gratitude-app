import React from "react";
import styles from "./Hero.scss";

const Hero = () => (
  <header className={styles.component}>
    <h2 className={styles.title}>I'm grateful for:</h2>
  </header>
);

export default Hero;
